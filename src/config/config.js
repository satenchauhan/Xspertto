import {
    API_URL,
    MAGIC_API_KEY,
    MAGIC_SECRET_KEY,
    WEB_CLIENT_ID,
    ANDROID_CLIENT_ID,
    IOS_CLIENT_ID,
    FACEBOOK_APP_ID,
    DATABASE_NAME,
    DB_PASS
} from '@env';

const DEV_ENV = {
    API_URL,
    MAGIC_API_KEY,
    MAGIC_SECRET_KEY,
    WEB_CLIENT_ID,
    ANDROID_CLIENT_ID,
    IOS_CLIENT_ID,
    FACEBOOK_APP_ID,
    DATABASE_NAME,
    DB_PASS
};

export default {DEV_ENV};


// npm install react-native-dotenv --save-dev