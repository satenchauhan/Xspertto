import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const ClientFile = () => {
    return (
        <View>
            <Text>This is file screen</Text>
        </View>
    )
}

export default ClientFile;

const styles = StyleSheet.create({})
